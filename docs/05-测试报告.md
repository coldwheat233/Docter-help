# 05 - 测试报告（占位）

> 第 3 周填充。包含 20+ 测试用例的执行结果、指标统计、覆盖率。

## 计划内容

- 20+ 对话用例的执行结果（consultation / appointment / reschedule / cancel / edge cases）
- 意图路由准确率统计（目标 ≥ 90%）
- 预约流程完整率统计（目标 ≥ 85%）
- 关键操作人工确认率（必须 100%）
- 单次预约对话平均时间（目标 ≤ 3 分钟）
- 5 分钟 demo 脚本 + 录屏
- pytest 覆盖率报告

## 当前已有测试

- `tests/test_state.py`：State 字段导入 + 枚举值校验
- `tests/test_repositories.py`：5 个 Repository 的 CRUD 冒烟

## 已有用例骨架

- `tests/cases/01_consultation.json` - 纯咨询
- `tests/cases/02_appointment_full.json` - 完整预约
- `tests/cases/03_appointment_missing_symptom.json` - 信息不全
- `tests/cases/04_reschedule.json` - 改约
- `tests/cases/05_cancel.json` - 取消

## 第 3 周补全

- 编写 15+ 额外用例
- 编写用例执行器（pytest 参数化 + mock LLM）
- 统计指标并出报告
- 跑 5 分钟 demo 录屏
